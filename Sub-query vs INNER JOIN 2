SELECT n.CountryName, 
       c.BiggestCity 
FROM Nations AS n
INNER JOIN 
    (SELECT CountryCode, 
     MAX(Pop2017) AS BiggestCity 
     FROM Cities
     GROUP BY CountryCode) AS c
ON n.Code2 = c.CountryCode;
